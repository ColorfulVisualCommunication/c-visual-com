#Routes (app/routes)
#These define API endpoints and connect requests to the controller.

#Example (auth_routes.py):
    #@auth_bp.route('/login', methods=['POST'])
    #def login():
    #    return login_user()

  #Where login_user() is from auth_controller.py.  
